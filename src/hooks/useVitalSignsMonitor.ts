import { useEffect } from 'react';
import toast from 'react-hot-toast';

export const useVitalSignsMonitor = () => {
  useEffect(() => {
    // Vital signs monitoring disabled per user request
  }, []);
};
